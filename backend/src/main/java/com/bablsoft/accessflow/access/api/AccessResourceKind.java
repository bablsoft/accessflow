package com.bablsoft.accessflow.access.api;

/** The kind of resource an access-grant request targets (exactly one per request). */
public enum AccessResourceKind {
    DATASOURCE,
    API_CONNECTOR
}
