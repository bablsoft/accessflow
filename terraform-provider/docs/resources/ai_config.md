---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "accessflow_ai_config Resource - accessflow"
subcategory: ""
description: |-
  An AI analyzer configuration (provider + model + connection). The API key is write-only.
---

# accessflow_ai_config (Resource)

An AI analyzer configuration (provider + model + connection). The API key is write-only.

## Example Usage

```terraform
resource "accessflow_ai_config" "claude" {
  name        = "claude"
  ai_provider = "ANTHROPIC" # `provider` is reserved by Terraform, so the attribute is `ai_provider`
  model       = "claude-sonnet-4-20250514"
  api_key     = var.anthropic_api_key # write-only
}

variable "anthropic_api_key" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `ai_provider` (String) `OPENAI`, `ANTHROPIC`, `OLLAMA`, `OPENAI_COMPATIBLE`, or `HUGGING_FACE`. (Named `ai_provider` because `provider` is a reserved Terraform argument.)
- `model` (String)
- `name` (String)

### Optional

- `api_key` (String, Sensitive) Write-only. Never returned by the API, so changes are applied but not detected as drift.
- `endpoint` (String)
- `max_completion_tokens` (Number)
- `max_prompt_tokens` (Number)
- `system_prompt_template` (String) Custom analyzer prompt; must contain `{{sql}}`. Omit for the built-in default.
- `timeout_ms` (Number)

### Read-Only

- `id` (String) The ID of this resource.
- `organization_id` (String)
