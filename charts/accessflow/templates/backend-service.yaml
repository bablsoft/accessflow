apiVersion: v1
kind: Service
metadata:
  name: {{ include "accessflow.backend.fullname" . }}
  labels:
    {{- include "accessflow.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accessflow.backend.selectorLabels" . | nindent 4 }}
