target/
*.class
*.jar
*.war
*.ear
*.log

# E2E custom-driver fixtures (AF-274) — committed binaries required by
# tests/admin-custom-drivers.spec.ts. Regeneration recipe lives in
# e2e/fixtures/drivers/README.md.
!e2e/fixtures/drivers/*.jar

# IntelliJ IDEA
.idea/
*.iml

# VS Code
.vscode/
.history/

# OS X
.DS_Store

# Node — each package has its own .gitignore, but a stray root-level node_modules/ (a tool
# run from the repo root rather than from frontend/ or e2e/) was otherwise committable.
node_modules/

# Docker
.docker/
build/

# Secrets / keys
*.pem
*.key
.env
.env.local
frontend/.claude/
.claude/worktrees/

# Helm chart dependency artefacts (re-fetched by `helm dependency update`)
charts/*/charts/
charts/*/Chart.lock
charts/*/*.tgz
